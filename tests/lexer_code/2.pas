var
        a,b: real;
        r: real;
    begin
        {
            многостройчный
            комментарий
        }
        write('Введите координату точки a: ');
        readln(a);
        a := 123.4
        write('Введите координату точки b: ');
        readln(b);
        r := abs(a-b);
        writeln('Расстояние между точками = ', r);
    end.